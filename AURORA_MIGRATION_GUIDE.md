# Руководство по переносу компонента Aurora

## Описание

Компонент `Aurora.tsx` создает красивый анимированный WebGL эффект Aurora (северное сияние) с использованием библиотеки OGL. Эффект использует шейдеры для создания плавных градиентных волн.

## Шаг 1: Установка зависимостей

### Для npm:
```bash
npm install ogl
```

### Для yarn:
```bash
yarn add ogl
```

### Для pnpm:
```bash
pnpm add ogl
```

## Шаг 2: Копирование файла

Скопируйте файл `src/components/Aurora.tsx` в ваш проект в папку `src/components/` (или аналогичную структуру компонентов).

## Шаг 3: Структура компонента

Компонент состоит из:
- **Вершинного шейдера (VERT)** - простая геометрия треугольника
- **Фрагментного шейдера (FRAG)** - сложный шейдер с noise функцией и градиентами
- **React компонента** - управление WebGL контекстом и анимацией

## Шаг 4: Использование компонента

### Базовое использование:

```tsx
import Aurora from './components/Aurora';

function App() {
  return (
    <div className="relative w-full h-screen">
      <Aurora />
    </div>
  );
}
```

### С кастомными параметрами:

```tsx
import Aurora from './components/Aurora';

function App() {
  return (
    <div className="relative w-full h-screen">
      <Aurora 
        colorStops={['#82D5FF', '#8B8EFF', '#7E3FFC', '#AB57FF', '#D35EF8', '#FF66EF']}
        amplitude={0.8}
        blend={0.5}
      />
    </div>
  );
}
```

## Шаг 5: Параметры компонента

### Props интерфейс:

```typescript
interface AuroraProps {
  colorStops?: string[];    // Массив цветов для градиента (hex)
  amplitude?: number;       // Амплитуда волн (по умолчанию: 1.0)
  blend?: number;           // Размытие краев (по умолчанию: 0.7)
  time?: number;           // Время для анимации (опционально)
  speed?: number;          // Скорость анимации (по умолчанию: 1.0)
}
```

### Параметры по умолчанию:

```typescript
colorStops: ['#016fee', '#a278f8', '#41c97c']  // Синий, фиолетовый, зеленый
amplitude: 1.0                                  // Стандартная амплитуда
blend: 0.7                                      // Среднее размытие
speed: 1.0                                      // Нормальная скорость
```

## Шаг 6: Интеграция в проект

### Вариант 1: Как фон страницы

```tsx
function App() {
  return (
    <div className="relative min-h-screen">
      {/* Aurora эффект на фоне */}
      <div className="absolute inset-0 opacity-20">
        <Aurora 
          colorStops={['#82D5FF', '#8B8EFF', '#7E3FFC', '#AB57FF', '#D35EF8', '#FF66EF']}
          amplitude={0.8}
          blend={0.5}
        />
      </div>
      
      {/* Контент поверх эффекта */}
      <div className="relative z-10">
        {/* Ваш контент */}
      </div>
    </div>
  );
}
```

### Вариант 2: Внутри контейнера

```tsx
function Section() {
  return (
    <section className="relative h-[500px] overflow-hidden">
      <Aurora 
        colorStops={['#FF0000', '#00FF00', '#0000FF']}
        amplitude={1.2}
        blend={0.8}
      />
      <div className="relative z-10 p-8">
        {/* Контент */}
      </div>
    </section>
  );
}
```

## Шаг 7: Стилизация

Компонент автоматически занимает весь родительский контейнер. Используйте CSS для позиционирования:

```css
/* Контейнер для Aurora */
.aurora-container {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none; /* Чтобы не блокировать клики */
  z-index: 0; /* Под контентом */
}

/* Контент поверх Aurora */
.content {
  position: relative;
  z-index: 10; /* Над Aurora */
}
```

Или используйте Tailwind классы:

```tsx
<div className="absolute inset-0 pointer-events-none">
  <Aurora />
</div>
```

## Шаг 8: Оптимизация производительности

### Для мобильных устройств:

```tsx
const [isMobile, setIsMobile] = useState(false);

useEffect(() => {
  setIsMobile(window.innerWidth < 768);
}, []);

return (
  <div className="relative">
    {!isMobile && (
      <Aurora 
        amplitude={0.6}  // Меньшая амплитуда для экономии ресурсов
        blend={0.4}
      />
    )}
  </div>
);
```

### Отключение при неактивной вкладке:

```tsx
const [isVisible, setIsVisible] = useState(true);

useEffect(() => {
  const handleVisibilityChange = () => {
    setIsVisible(!document.hidden);
  };
  
  document.addEventListener('visibilitychange', handleVisibilityChange);
  return () => document.removeEventListener('visibilitychange', handleVisibilityChange);
}, []);

return (
  <div className="relative">
    {isVisible && <Aurora />}
  </div>
);
```

## Шаг 9: Кастомизация цветов

### Примеры цветовых схем:

**Океан:**
```tsx
<Aurora 
  colorStops={['#00D4FF', '#0099CC', '#0066FF', '#0033CC']}
/>
```

**Закат:**
```tsx
<Aurora 
  colorStops={['#FF6B6B', '#FF8E53', '#FF6B9D', '#C44569']}
/>
```

**Лес:**
```tsx
<Aurora 
  colorStops={['#51CF66', '#40C057', '#37B24D', '#2F9E44']}
/>
```

**Неон:**
```tsx
<Aurora 
  colorStops={['#FF00FF', '#00FFFF', '#FFFF00', '#FF00FF']}
/>
```

## Шаг 10: Решение проблем

### Проблема: Aurora не отображается

**Решение:**
1. Убедитесь, что контейнер имеет фиксированные размеры (`width` и `height`)
2. Проверьте, что родительский элемент имеет `position: relative`
3. Убедитесь, что библиотека `ogl` установлена

```tsx
// ❌ Неправильно - нет размеров
<div>
  <Aurora />
</div>

// ✅ Правильно - есть размеры
<div className="w-full h-screen relative">
  <Aurora />
</div>
```

### Проблема: Низкая производительность

**Решение:**
1. Уменьшите `amplitude` (например, до 0.5)
2. Уменьшите `blend` (например, до 0.3)
3. Используйте меньше цветов в `colorStops` (3-4 вместо 6)
4. Отключите на мобильных устройствах

### Проблема: Конфликт с другими WebGL элементами

**Решение:**
Создайте отдельный WebGL контекст для Aurora:

```tsx
// Компонент автоматически создает свой контекст
// Но если есть конфликты, оберните в отдельный контейнер
<div id="aurora-container">
  <Aurora />
</div>
```

### Проблема: TypeScript ошибки

**Решение:**
Убедитесь, что типы для `ogl` установлены:

```bash
npm install --save-dev @types/node
```

Если типов нет, создайте файл `types/ogl.d.ts`:

```typescript
declare module 'ogl' {
  export class Renderer {
    gl: WebGLRenderingContext;
    constructor(options?: any);
    setSize(width: number, height: number): void;
    render(options: { scene: Mesh }): void;
  }
  
  export class Program {
    uniforms: any;
    constructor(gl: WebGLRenderingContext, options: any);
  }
  
  export class Mesh {
    constructor(gl: WebGLRenderingContext, options: any);
  }
  
  export class Triangle {
    constructor(gl: WebGLRenderingContext);
    attributes: any;
  }
  
  export class Color {
    r: number;
    g: number;
    b: number;
    constructor(color: string);
  }
}
```

## Шаг 11: Пример полной интеграции

```tsx
import { useState, useEffect } from 'react';
import Aurora from './components/Aurora';

function LandingPage() {
  const [mounted, setMounted] = useState(false);

  useEffect(() => {
    setMounted(true);
  }, []);

  return (
    <div className="relative min-h-screen w-full overflow-hidden bg-gradient-to-br from-gray-50 to-white">
      {/* Aurora эффект */}
      {mounted && (
        <div className="absolute inset-0 opacity-20">
          <Aurora 
            colorStops={['#82D5FF', '#8B8EFF', '#7E3FFC', '#AB57FF', '#D35EF8', '#FF66EF']}
            amplitude={0.8}
            blend={0.5}
          />
        </div>
      )}

      {/* Контент */}
      <div className="relative z-10 flex flex-col items-center justify-center min-h-screen p-4">
        <h1 className="text-4xl font-bold text-gray-900 mb-4">
          Добро пожаловать
        </h1>
        {/* Остальной контент */}
      </div>
    </div>
  );
}

export default LandingPage;
```

## Шаг 12: Проверка работоспособности

После интеграции проверьте:

1. ✅ Компонент рендерится без ошибок
2. ✅ Анимация плавная (60 FPS)
3. ✅ Цвета отображаются корректно
4. ✅ Эффект адаптируется к размеру экрана
5. ✅ Нет конфликтов с другими элементами
6. ✅ Производительность приемлемая

## Дополнительные ресурсы

- **Документация OGL:** https://github.com/oframe/ogl
- **WebGL шейдеры:** https://webglfundamentals.org/
- **GLSL синтаксис:** https://www.khronos.org/opengl/wiki/OpenGL_Shading_Language

## Важные замечания

1. **Производительность:** Aurora использует WebGL и может быть ресурсоемким на слабых устройствах
2. **Совместимость:** Требует поддержку WebGL (все современные браузеры)
3. **Размер:** Компонент добавляет ~15KB к размеру бандла (с ogl)
4. **Анимация:** Использует `requestAnimationFrame` для плавной анимации
5. **Очистка:** Компонент автоматически очищает ресурсы при размонтировании

## Быстрый старт (чеклист)

- [ ] Установить `ogl`: `npm install ogl`
- [ ] Скопировать `Aurora.tsx` в проект
- [ ] Импортировать компонент
- [ ] Обернуть в контейнер с размерами
- [ ] Настроить цвета и параметры
- [ ] Протестировать на разных устройствах
- [ ] Оптимизировать производительность при необходимости

Готово! Теперь у вас есть красивый Aurora эффект в вашем проекте. 🎨✨
