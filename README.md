# Tap Link - Красивый лендинг с отслеживанием кликов

Современный лендинг на React + Vite с интеграцией Google Sheets для отслеживания кликов по ссылкам.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка для продакшена

```bash
npm run build
```

## 📊 Настройка отслеживания через Google Sheets

### Шаг 1: Создание Google Таблицы и Apps Script

1. Создайте новую Google Таблицу
2. В верхнем меню выберите **Расширения (Extensions) → Apps Script**
3. Вставьте следующий код:

```javascript
function doPost(e) {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  var data = JSON.parse(e.postData.contents);
  
  // Добавляем строку: Дата, Название ссылки, Счетчик (1)
  sheet.appendRow([new Date(), data.linkName, 1]);
  
  return ContentService.createTextOutput(JSON.stringify({"result":"success"}))
    .setMimeType(ContentService.MimeType.JSON);
}
```

4. Нажмите **Deploy (Начать развертывание) → New Deployment**
5. Выберите тип **Web App**
6. В поле "Who has access" выберите **Anyone** (чтобы фронтенд мог отправлять данные без авторизации)
7. Скопируйте полученный **Web App URL**

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_GOOGLE_SCRIPT_URL=ваш_web_app_url_из_шага_1
```

Или отредактируйте `src/config.ts` и укажите URL напрямую:

```typescript
export const GOOGLE_SCRIPT_URL = 'ваш_web_app_url';
```

### Шаг 3: Настройка ссылок

Отредактируйте файл `src/config.ts` и измените массив `links`:

```typescript
export const links: Link[] = [
  {
    id: '1',
    name: 'Профиль',
    url: 'https://example.com/profile',
    icon: '👤',
  },
  // ... другие ссылки
];
```

## 🎨 Особенности

- ✨ Современный градиентный дизайн
- 📱 Адаптивная верстка
- 🎭 Плавные анимации и переходы
- 📊 Отслеживание кликов через Google Sheets
- 🚀 Быстрая загрузка благодаря Vite
- 💅 Tailwind CSS для стилизации

## 📁 Структура проекта

```
tap-link/
├── src/
│   ├── components/      # React компоненты
│   │   └── LinkButton.tsx
│   ├── config.ts        # Конфигурация и данные ссылок
│   ├── utils/           # Утилиты
│   │   └── tracking.ts  # Функция отслеживания кликов
│   ├── App.tsx          # Главный компонент
│   └── main.tsx         # Точка входа
├── public/              # Статические файлы
└── package.json
```

## 🔧 Технологии

- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик и dev-сервер
- **Tailwind CSS** - Стилизация
- **Google Apps Script** - Backend для отслеживания

## 📝 Лицензия

MIT
